<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.bfchengnuo.systemservertest.MainActivity">

    <Button
        android:id="@+id/checkIntent"
        android:onClick="doClick"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="检测网络状态"/>

    <Button
        android:id="@+id/checkWifi"
        android:onClick="doClick"
        android:layout_below="@id/checkIntent"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="开启/关闭WIFI"/>
    <Button
        android:id="@+id/volume"
        android:onClick="doClick"
        android:layout_below="@id/checkWifi"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="显示系统音量"/>
    <Button
        android:id="@+id/time"
        android:onClick="doClick"
        android:layout_below="@id/volume"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="启动一个定时服务"/>
</RelativeLayout>
